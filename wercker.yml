box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        theme: hugo-zen
        flags: --buildDrafts=true
deploy:
  steps:
    - git-push:
        gh_oauth: $GITHUB_TOKEN
        gh_pages: true
        basedir: public
