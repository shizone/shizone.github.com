box: golang
pre:
  steps:
    - script:
        name: output message
        code: |
          echo 'start to build & deploy'
build:
  steps:
    - arjen/hugo-build:
        version: 0.18
        theme: hugo-zen
deploy:
  steps:
    - leipert/git-push:
        gh_oauth: $GITHUB_TOKEN
        gh_pages: true
        basedir: public
